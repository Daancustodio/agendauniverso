// JavaScript Document
function verificar(){
	var senha = window.document.getElementById(senha);
	var confirmarSenha = window.document.getElementById(confirmarsenha);
	
	if (senha != conformarSenha){
		alert("As senhas não conferem");
		} 	
}